# render.yamls
services:
  - type: web
    name: hr-mcp-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.web_server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0