services:
  - type: web
    name: hr-mcp-server
    env: docker
    plan: free
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: MCP_SERVER_NAME
        value: hr-mcp-server
      - key: MCP_SERVER_VERSION
        value: 1.0.0