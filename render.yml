# render.yaml
services:
  - type: web
    name: hr-mcp-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python src.web_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0